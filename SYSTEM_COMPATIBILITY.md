# 시스템 호환성 및 개선 사항

## 현재 상태 분석

### ✅ 정상 동작하는 부분

1. **센서 데이터 전송**

   - 아두이노 → 서버: `POST /api/sensors` (Public)
   - 전송 데이터: `module_id`, `temperature`, `humidity`
   - 전송 주기: 30초
   - ✅ 정상 동작

2. **액추에이터 상태 조회**

   - 아두이노 → 서버: `GET /api/actuators/status/:moduleId` (Public으로 변경됨)
   - 조회 주기: 10초
   - ✅ 정상 동작

3. **모듈 관리**
   - 웹 대시보드에서 모듈 등록/조회/삭제
   - 최대 30개 모듈 제한
   - ✅ 정상 동작

### 🔧 개선된 사항

1. **액추에이터 상태 조회 API Public화**

   - **문제**: 아두이노가 인증 없이 호출해야 하는데 Private 엔드포인트였음
   - **해결**: `GET /api/actuators/status/:moduleId`를 Public으로 변경
   - **추가**: 웹 대시보드용 Private 엔드포인트 `GET /api/actuators/:moduleId` 추가

2. **모듈 생성 시 relay 필드 초기화**
   - **문제**: 모듈 생성 시 actuator_status에 relay 필드가 초기화되지 않음
   - **해결**: `relay: false` 추가

## 시스템 아키텍처 호환성

### 데이터 흐름

```
아두이노 R4 (모듈 1~30)
  ↓ (30초마다)
센서 데이터 전송: POST /api/sensors
  ↓
서버 (AWS Lightsail)
  ↓
데이터베이스 (AWS RDS)
  ↓
웹 대시보드 (Vercel)
  ↑ (10초마다)
액추에이터 상태 조회: GET /api/actuators/status/:moduleId
  ↑
아두이노 R4
```

### API 엔드포인트 정리

#### Public (아두이노에서 호출)

| 엔드포인트                        | 메서드 | 설명                 | 호출 주기 |
| --------------------------------- | ------ | -------------------- | --------- |
| `/api/sensors`                    | POST   | 센서 데이터 전송     | 30초      |
| `/api/actuators/status/:moduleId` | GET    | 액추에이터 상태 조회 | 10초      |

#### Private (웹 대시보드에서 호출)

| 엔드포인트                               | 메서드 | 설명                             |
| ---------------------------------------- | ------ | -------------------------------- |
| `/api/modules`                           | GET    | 모듈 목록 조회                   |
| `/api/modules`                           | POST   | 모듈 등록                        |
| `/api/modules/:moduleId`                 | GET    | 모듈 정보 조회                   |
| `/api/modules/:moduleId`                 | PUT    | 모듈 정보 수정                   |
| `/api/modules/:moduleId`                 | DELETE | 모듈 삭제                        |
| `/api/sensors/latest/:moduleId`          | GET    | 최신 센서 데이터 조회            |
| `/api/sensors/history/:moduleId`         | GET    | 센서 데이터 히스토리             |
| `/api/actuators/:moduleId`               | GET    | 액추에이터 상태 조회 (인증 필요) |
| `/api/actuators/:moduleId/:actuatorType` | POST   | 액추에이터 제어                  |

## 현재 구현 상태

### 하드웨어 (테스트 버전)

**현재 사용 중**:

- ✅ DHT11 센서 (온도/습도)
- ✅ 1채널 릴레이 모듈

**README.md에 언급된 전체 구성** (향후 확장 가능):

- 워터 레벨 센서
- DS18B20 온도 센서
- DO 센서 (SEN0237-A)
- pH 센서
- Grove Light Sensor
- Water Pump, Air Pump, Valve, Heater, Cooler

### 데이터베이스 스키마

**sensor_data 테이블**:

- ✅ `temperature` (DHT11)
- ✅ `humidity` (DHT11)
- ⚠️ `water_level`, `do_level`, `ph_level`, `light_level` (현재 미사용, 향후 확장용)

**actuator_status 테이블**:

- ✅ `relay` (현재 사용 중)
- ⚠️ `water_pump`, `air_pump`, `valve`, `heater`, `cooler` (향후 확장용)

## 스마트팜 시스템 요구사항 충족도

### ✅ 충족된 요구사항

1. **다중 모듈 지원**: 최대 30개 모듈 등록 가능
2. **실시간 모니터링**: 30초마다 센서 데이터 업데이트
3. **원격 제어**: 웹 대시보드에서 액추에이터 제어
4. **사용자 인증**: 로그인 기반 모듈 관리
5. **데이터 저장**: 센서 데이터 히스토리 저장
6. **WiFi 설정**: AP 모드로 웹 기반 WiFi 설정

### 🔄 향후 확장 가능한 기능

1. **자동 제어**: 임계값 기반 자동 제어 (현재는 서버 제어만)
2. **알림 기능**: 센서 값 임계값 초과 시 알림
3. **차트 시각화**: 센서 데이터 히스토리 차트
4. **다중 센서**: 5개 센서 전체 지원
5. **다중 액추에이터**: 5개 액추에이터 전체 지원

## 개선 권장 사항

### 1. 에러 처리 강화

**아두이노 코드**:

- 서버 응답 상태 코드 확인
- 재시도 로직 추가
- 오류 로깅 개선

**서버 코드**:

- 입력 데이터 검증 강화
- 타임아웃 처리
- 에러 메시지 상세화

### 2. 보안 강화

- 모듈 ID 기반 접근 제어 (현재는 Public이지만 모듈 존재 여부 확인)
- API Rate Limiting 추가
- HTTPS 강제 (현재는 HTTP 사용)

### 3. 성능 최적화

- 데이터베이스 인덱스 최적화
- 배치 처리 (여러 센서 데이터 한 번에 전송)
- 캐싱 (최신 센서 데이터 캐싱)

### 4. 모니터링

- 서버 상태 모니터링
- 모듈 연결 상태 추적
- 데이터 전송 실패 알림

## 테스트 체크리스트

### 아두이노 테스트

- [x] WiFi 연결 및 재연결
- [x] 센서 데이터 전송 (30초 주기)
- [x] 액추에이터 상태 조회 (10초 주기)
- [x] 릴레이 제어 동작

### 서버 테스트

- [x] 센서 데이터 수신 및 저장
- [x] 액추에이터 상태 조회 (Public)
- [x] 모듈 등록/조회/삭제
- [x] 사용자 인증

### 통합 테스트

- [x] 아두이노 → 서버 데이터 전송
- [x] 웹 대시보드 → 서버 → 아두이노 제어
- [x] 다중 모듈 동시 운영

## 결론

현재 시스템은 **스마트팜 시스템의 기본 요구사항을 충족**하고 있으며, **테스트 버전으로 안정적으로 동작**합니다.

**주요 개선 사항**:

1. ✅ 액추에이터 상태 조회 API Public화 완료
2. ✅ 모듈 생성 시 relay 필드 초기화 완료
3. ✅ 아두이노와 서버 간 API 호환성 확인 완료

**향후 확장 시 고려사항**:

- 전체 센서/액추에이터 지원을 위한 코드 확장
- 자동 제어 로직 구현
- 알림 및 모니터링 기능 추가
