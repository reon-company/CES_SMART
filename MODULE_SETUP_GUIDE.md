# 모듈 추가 가이드

## 모듈 추가 방법

### 1. 웹 대시보드에서 모듈 추가

1. **로그인**: 웹 대시보드(`dashboard.html`)에 로그인
2. **모듈 추가 버튼 클릭**: "+ 모듈 추가" 버튼 클릭
3. **정보 입력**:
   - **모듈 이름**: 관리하기 쉬운 이름 (예: "1호 수조", "테스트 모듈")
   - **Module ID**: 아두이노 코드의 `MODULE_ID`와 **정확히 일치**해야 함
     - 예: `MODULE_001`, `MODULE_002`, `MODULE_003` 등
     - **중요**: 이 ID는 아두이노 코드의 `config.h`에 설정한 `MODULE_ID`와 동일해야 합니다!

4. **추가 버튼 클릭**: 모듈이 등록됩니다

### 2. 아두이노 코드 설정

각 아두이노 모듈마다 고유한 `MODULE_ID`를 설정해야 합니다.

**설정 위치**: `arduino-r4/CES_SmartFarm/config.h`

```cpp
// Module Configuration
#define MODULE_ID "MODULE_001"  // 웹 대시보드에 등록한 Module ID와 일치해야 함!
```

**중요 사항**:
- 각 아두이노마다 다른 `MODULE_ID` 사용
- 웹 대시보드에 등록한 `Module ID`와 **정확히 일치**해야 함
- 대소문자 구분됨 (`MODULE_001` ≠ `module_001`)

### 3. 모듈 ID 확인 방법

**아두이노 시리얼 모니터**에서 확인:
```
========================================
CES SmartFarm Arduino R4 Starting
========================================
Module ID: MODULE_001
========================================
```

**웹 대시보드**에서 확인:
- 모듈 목록에 등록된 Module ID 표시
- 모듈 상세 페이지에서 확인 가능

## 모듈 추가 예시

### 예시 1: 첫 번째 모듈

**웹 대시보드**:
- 모듈 이름: "1호 수조"
- Module ID: `MODULE_001`

**아두이노 코드** (`config.h`):
```cpp
#define MODULE_ID "MODULE_001"
```

### 예시 2: 두 번째 모듈

**웹 대시보드**:
- 모듈 이름: "2호 수조"
- Module ID: `MODULE_002`

**아두이노 코드** (`config.h`):
```cpp
#define MODULE_ID "MODULE_002"
```

## 모듈 추가 후 확인

### 1. 센서 데이터 전송 확인

아두이노 시리얼 모니터에서:
```
Temperature: 25.3C | Humidity: 60.5%
Sensor data sent
```

**"Sensor data sent"** 메시지가 나타나면 정상입니다.

만약 **"Module not found"** 오류가 나타나면:
- 웹 대시보드에 모듈이 등록되어 있는지 확인
- 아두이노의 `MODULE_ID`와 웹 대시보드의 `Module ID`가 일치하는지 확인

### 2. 웹 대시보드에서 확인

1. 모듈 목록에 추가된 모듈이 표시되는지 확인
2. 모듈 상세 페이지에서 센서 데이터가 표시되는지 확인
3. 릴레이 제어가 동작하는지 확인

## 문제 해결

### "Module not found" 오류

**원인**:
- 웹 대시보드에 모듈이 등록되지 않음
- 아두이노의 `MODULE_ID`와 웹 대시보드의 `Module ID`가 일치하지 않음

**해결**:
1. 웹 대시보드에서 모듈이 등록되어 있는지 확인
2. 아두이노 코드의 `MODULE_ID` 확인
3. 두 값이 정확히 일치하는지 확인 (대소문자 포함)
4. 모듈을 다시 등록하거나 아두이노 코드 수정 후 재업로드

### "Module ID already exists" 오류

**원인**:
- 이미 같은 `Module ID`로 모듈이 등록되어 있음

**해결**:
1. 다른 `Module ID` 사용 (예: `MODULE_002`, `MODULE_003`)
2. 또는 기존 모듈 삭제 후 다시 등록

### 센서 데이터가 전송되지 않음

**확인 사항**:
1. WiFi 연결 상태 확인
2. 서버 주소 확인 (`config.h`의 `API_BASE_URL`)
3. 시리얼 모니터에서 오류 메시지 확인
4. 웹 대시보드에 모듈이 등록되어 있는지 확인

## 모듈 관리

### 모듈 수정

웹 대시보드에서 모듈 정보 수정 가능:
- 모듈 이름 변경
- Module ID는 변경 불가 (고유 식별자)

### 모듈 삭제

1. 웹 대시보드에서 모듈 선택
2. "삭제" 버튼 클릭
3. 확인 후 삭제

**주의**: 모듈 삭제 시 해당 모듈의 센서 데이터 히스토리도 함께 삭제됩니다.

### 모듈 제한

- 사용자당 최대 **30개 모듈** 등록 가능
- 모듈 개수 초과 시 "Maximum 30 modules allowed per user" 오류 발생

## 모듈 ID 명명 규칙 권장사항

**권장 형식**:
- `MODULE_001`, `MODULE_002`, `MODULE_003` (순차적)
- `FARM_001`, `FARM_002` (농장별)
- `TANK_01`, `TANK_02` (수조별)

**주의사항**:
- 영문자, 숫자, 언더스코어(`_`)만 사용
- 대소문자 구분됨
- 최대 50자
- 공백 사용 불가

## 전체 프로세스 요약

```
1. 아두이노 코드 작성
   └─ config.h에서 MODULE_ID 설정 (예: "MODULE_001")

2. 아두이노 코드 업로드
   └─ Arduino IDE에서 업로드

3. 웹 대시보드에서 모듈 등록
   └─ 모듈 이름: "1호 수조"
   └─ Module ID: "MODULE_001" (아두이노와 일치!)

4. 아두이노 부팅 및 WiFi 연결
   └─ 센서 데이터 자동 전송 시작 (30초마다)

5. 웹 대시보드에서 확인
   └─ 센서 데이터 표시
   └─ 릴레이 제어 가능
```

## 참고

- **모듈 등록은 웹 대시보드에서만 가능** (아두이노는 자동 등록 안 됨)
- **Module ID는 고유해야 함** (중복 불가)
- **아두이노와 서버의 Module ID가 일치해야 데이터 전송 가능**

